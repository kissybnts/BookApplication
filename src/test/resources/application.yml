spring:
# DataSource
  datasource:
    url: jdbc:mysql://localhost/book_application_test
    username: user
    password: user
    driver-class-name: com.mysql.jdbc.Driver
# JPA
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
# Thymeleaf
  thymeleaf:
    cache: false